# Core dependencies for VersaFace Autoencoder
# Python 3.8+ required

# PyTorch (CPU or CUDA - adjust based on your system)
# For CUDA 11.8: pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu118
# For CUDA 12.1: pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121
# For CPU only: pip install torch torchaudio
torch>=2.0.0,<2.3.0
torchaudio>=2.0.0,<2.3.0

# Audio processing
librosa>=0.10.0,<0.11.0
soundfile>=0.12.0

# HuggingFace (for models and transformers)
# IMPORTANT: transformers 4.40.0 is required for compatibility
transformers==4.40.0
huggingface-hub>=0.16.0
safetensors>=0.3.0

# Scientific computing
numpy>=1.24.0,<2.0.0
scipy>=1.6.0

# Utilities
einops>=0.6.0
json5>=0.9.0
ruamel.yaml>=0.18.0

# Optional: Jupyter for notebook examples
jupyter>=1.0.0
ipykernel>=6.0.0
notebook>=6.0.0

# Optional: For development
# pytest>=7.0.0
# black>=23.0.0
# flake8>=6.0.0
